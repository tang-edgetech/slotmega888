RewriteEngine On

# Only process if the request is not an actual file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Send all other requests to blogs.php as slug
RewriteRule ^([^/]+)/?$ blogs.php?slug=$1 [L,QSA]
